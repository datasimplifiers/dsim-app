# Data Simplifiers App

### Clone the Git Repository

		git clone https://github.com/datasimplifiers/dsim-app.git

### Start the docker containers using docker compose or docker-compose

		cd dsim-app
		docker compose up -d
OR
		cd dsim-app
		docker-compose up -d
